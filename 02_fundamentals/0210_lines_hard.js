function setup() {
    createCanvas(400, 400);
    background(220); // grey background
    
    // Set text properties
    textSize(48); // big text

    text("Sentence 1", 100, 100);
    text("Sentence 2", 100, 150);
    text("Sentence 3", 100, 200);
    text("Sentence 4", 100, 250);
    text("Sentence 5", 100, 300);
}

function draw() {
    // No animation needed for this demonstration
}
