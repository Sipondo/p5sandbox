function setup() {
    createCanvas(400, 400);
    background(220); // grey background
    strokeWeight(10); // how thick the point is
    point(200, 200);
}

function draw() {
    // Static drawing
}
